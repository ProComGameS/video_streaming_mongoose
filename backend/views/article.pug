doctype html
html
    head
        link(rel="icon", href="/favicon.ico")
        title= article.title
        link(rel="stylesheet", href="/styles.css")
    body
        body(class=theme)
            a(href='/toggle-theme', class='theme-toggle')
        h1= article.title
        p= article.content
        a(href="/articles") ← Назад до списку
